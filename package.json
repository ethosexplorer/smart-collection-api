{
  "name": "smart-collections-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test tests/",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio", 
    "db:generate": "drizzle-kit generate",
    "test:connection": "bun tests/real-connection.ts",
    "test:simple": "bun tests/test-simple.ts",
    "test:db": "bun tests/test-simple.ts"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "drizzle-orm": "^0.30.0",
    "postgres": "^3.4.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.21.0"
  }
}